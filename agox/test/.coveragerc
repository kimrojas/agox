[run]
source = ../acquisitors, ../candidates, ../collectors, ../databases, ../environments, ../evaluators, ../generators, ../helpers, ../models, ../postprocessors, ../samplers, ../utils, ../

[report]
# Regexes for lines to exclude from consideration
exclude_lines =

    # Don't complain about abstract methods, they aren't run:
    @(abc\.)?abstractmethod
